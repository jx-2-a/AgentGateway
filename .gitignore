# ============ Python ============
__pycache__/
*.py[cod]
.venv/
venv/

# ============ 敏感配置（绝不入库） ============
# 网关 token / ttyd 路径 / 公网地址等本地配置
.env
.env.*
!.env.example

# Gotify 通知服务的 token
gotif/token

# ============ Claude Code 本地设置 ============
.claude/

# ============ 个人笔记 ============
work.md

# ============ 日志与运行产物 ============
*.log
.runtime/
data/   # 运行时状态（通知开关等，不入库）
